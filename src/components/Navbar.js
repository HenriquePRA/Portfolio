import React from 'react'

const Navbar = () => {
    return (
        <div id="navbar">
            <button>Sobre</button>
            <button>Projetos</button>
            <button>Contato</button>
        </div>
    )
}

export default Navbar;